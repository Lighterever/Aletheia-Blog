# 内容混淆规范

## 概述
将 `data.js` 和 `timeline-data.js` 中的文章内容字段进行 base64 编码，防止在 GitHub 仓库中直接阅读原始内容。这是轻量级混淆方案，防浏览不防破解。

## 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `scripts/build-articles.js` | 修改 | content 字段输出 base64 |
| `js/utils.js` 或 `app.js` | 修改 | 新增 decodeContent 函数 |
| 文章渲染处 | 修改 | content → decodeContent(content) |
| `.gitignore` | 修改 | 新增 articles/ 和 timeline/ |

---

## 步骤 1：修改构建脚本

**文件**：`scripts/build-articles.js`

### 1.1 generateDataJs() 函数

**位置**：`entries.map()` 回调中，`content` 字段赋值处

**原代码**：
```javascript
content: \`${escapedContent}\`
```

**改为**：
```javascript
content: '${Buffer.from(a.content, 'utf-8').toString('base64')}'
```

**说明**：
- base64 输出只含 `A-Za-z0-9+/=`，不含反引号和 `${}`，无需 escapeTemplateLiteral
- 用单引号包裹，不需要模板字符串

### 1.2 generateTimelineJs() 函数

**位置**：`timelineEntries.map()` 回调中，`content` 字段赋值处

**原代码**：
```javascript
content: \`${escapeTemplateLiteral(e.content)}\`
```

**改为**：
```javascript
content: '${Buffer.from(e.content, 'utf-8').toString('base64')}'
```

### 1.3 验证

运行 `node scripts/build-articles.js`，检查输出的 data.js 中 content 字段为 base64 字符串，非明文中文。

---

## 步骤 2：前端解码函数

**文件**：`js/utils.js`（如已拆分为 ES Module）或 `app.js` 工具函数区域

**新增函数**：
```javascript
function decodeContent(str) {
    if (!str) return '';
    try {
        return decodeURIComponent(escape(atob(str)));
    } catch {
        return str; // 兼容未编码的旧数据或纯 ASCII
    }
}
```

**说明**：
- `atob()` 是浏览器原生 API，无需引入库
- `decodeURIComponent(escape(...))` 处理中文 base64 的解码（atob 返回 Latin1，中文需额外转换）
- catch 兜底：如果解码失败，返回原始字符串（兼容未编码数据）

**ES Module 导出**（如适用）：
```javascript
export function decodeContent(str) { ... }
```

---

## 步骤 3：渲染处替换

所有读取 `article.content` 或 `entry.content` 并展示到页面的地方，替换为 `decodeContent(content)`。

### 3.1 文章阅读页

**位置**：ArticleReader 中设置 innerHTML 的地方

```javascript
// 原
element.innerHTML = article.content;
// 改
element.innerHTML = decodeContent(article.content);
```

### 3.2 时间轴节点卡片

**位置**：TimelineCanvas 中渲染节点内容的地方

```javascript
// 原
element.innerHTML = entry.content;
// 改
element.innerHTML = decodeContent(entry.content);
```

### 3.3 来信页信封展开内容

**位置**：LettersPage 中 extractExcerpt 或渲染信件内容的地方

```javascript
// extractExcerpt 内部先解码再截取
function extractExcerpt(content, maxParagraphs) {
    const decoded = decodeContent(content);
    // ... 后续逻辑不变
}
```

---

## 步骤 4：.gitignore 更新

**文件**：`.gitignore`

**新增**：
```
# 源文件不推送，只推送构建产物
articles/
timeline/
```

---

## 验证清单

- [ ] `node scripts/build-articles.js` 运行成功
- [ ] `data.js` 中 content 字段为 base64 字符串，非明文中文
- [ ] `timeline-data.js` 中 content 字段为 base64 字符串，非明文中文
- [ ] 网站文章阅读页正常显示中文内容
- [ ] 网站时间轴节点卡片正常显示中文内容
- [ ] 网站来信页信封展开正常显示中文内容
- [ ] GitHub 仓库中 articles/ 和 timeline/ 目录不存在
- [ ] GitHub 仓库中 data.js 和 timeline-data.js 的 content 不可直接阅读

## 注意事项

- 这是**混淆不是加密**，base64 可以被任何人解码，目的只是防止 GitHub 上随手浏览
- 密钥室文章的加密（AES-256-CBC）不受影响，那是真正的加密
- 构建脚本修改后，旧的 data.js 需要重新生成
- `decodeContent` 的 catch 兜底确保向后兼容，不会因为旧数据格式导致页面报错
